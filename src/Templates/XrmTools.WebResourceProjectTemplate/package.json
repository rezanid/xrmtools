{
  "name": "dataverse-web-resources",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:debug",
    "build:debug": "tsc -p tsconfig.json --sourceMap true",
    "build:release": "tsc -p tsconfig.json --sourceMap false",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/xrm": "^9.0.88",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
