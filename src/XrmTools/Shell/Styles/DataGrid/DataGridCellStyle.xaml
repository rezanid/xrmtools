<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrls="clr-namespace:XrmTools.Shell.Controls"
                    xmlns:styles="clr-namespace:XrmTools.Shell.Styles">
  <Style TargetType="{x:Type ctrls:DataGridCell}">
    <Setter Property="Control.Background" Value="{DynamicResource {x:Static styles:ShellColors.ControlFillTransparentBrushKey}}"/>
    <Setter Property="Control.BorderBrush" Value="{DynamicResource {x:Static styles:ShellColors.ControlStrokeTransparentBrushKey}}"/>
    <Setter Property="Control.BorderThickness" Value="{x:Static styles:StrokeWidths.None}"/>
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Control.HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="ctrls:DataGridCell.Owner" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type ctrls:DataGrid}}, FallbackValue={x:Null}}"/>
    <Setter Property="Control.VerticalContentAlignment" Value="Center"/>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:DataGridCell}">
          <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}"
                  BorderThickness="{TemplateBinding Control.BorderThickness}">
            <Grid MinHeight="{x:Static styles:Sizes.ControlMinHeight}">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <ctrls:Indicator x:Name="Indicator" Grid.Column="0" Visibility="Hidden"/>
              <ContentPresenter x:Name="Content" Grid.Column="1"
                                HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                MinHeight="{x:Static styles:Sizes.ContentMinHeight}" MinWidth="{x:Static styles:Sizes.ControlMinWidth}"
                                VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"/>
              <Path x:Name="EditGlyph" Grid.Column="2" Data="{x:Static styles:Geometries.EditSmall}"
                    Fill="{TemplateBinding Control.Foreground}" Height="{x:Static styles:Sizes.IconS}"
                    VerticalAlignment="Center" Width="{x:Static styles:Sizes.IconS}" Visibility="Hidden"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="DataGridCell.IsEditing" Value="True">
              <Setter TargetName="EditGlyph" Property="UIElement.Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="UIElement.IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Indicator" Property="UIElement.Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="DataGridCell.IsReadOnly" Value="True">
              <Setter TargetName="Content" Value="{x:Static styles:Spacings.RightXS}" Property="FrameworkElement.Margin"/>
              <Setter TargetName="EditGlyph" Property="UIElement.Visibility" Value="Collapsed"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="UIElement.IsEnabled" Value="False">
        <Setter Property="Control.Foreground" Value="{DynamicResource {x:Static styles:ShellColors.TextFillDisabledBrushKey}}"/>
      </Trigger>
      <Trigger Property="UIElement.IsKeyboardFocused" Value="True">
        <Setter Property="Control.Background" Value="{DynamicResource {x:Static styles:ShellColors.SubtleFillSecondaryBrushKey}}"/>
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>
