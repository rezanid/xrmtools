<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrls="clr-namespace:XrmTools.Shell.Controls"
                    xmlns:styles="clr-namespace:XrmTools.Shell.Styles">
  <Style BasedOn="{x:Static styles:ShellInternalStyles.Button}" TargetType="{x:Type ctrls:DropDownButton}">
    <Setter Property="ctrls:DropDownButton.ShowDropDownGlyph" Value="True"/>
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrls:DropDownButton}">
          <Border Background="{TemplateBinding Control.Background}"
                  BorderBrush="{TemplateBinding Control.BorderBrush}"
                  BorderThickness="{TemplateBinding Control.BorderThickness}"
                  CornerRadius="{TemplateBinding ctrls:Button.CornerRadius}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <ContentPresenter Grid.Column="0" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                Margin="{TemplateBinding Control.Padding}" MinHeight="{x:Static styles:Sizes.ContentMinHeight}"
                                RecognizesAccessKey="True" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"/>
              <Path x:Name="Arrow" Grid.Column="1" Fill="{TemplateBinding Control.Foreground}"
                    Height="{x:Static styles:Sizes.IconS}" HorizontalAlignment="Center"
                    Margin="{x:Static styles:Spacings.RightXS}" VerticalAlignment="Center"
                    Width="{x:Static styles:Sizes.IconS}"/>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="ctrls:DropDownButton.Placement" Value="Bottom">
              <Setter TargetName="Arrow" Value="{x:Static styles:Geometries.ChevronDownSmall}" Property="Path.Data"/>
            </Trigger>
            <Trigger Property="ctrls:DropDownButton.Placement" Value="Left">
              <Setter TargetName="Arrow" Value="{x:Static styles:Geometries.ChevronLeftSmall}" Property="Path.Data"/>
            </Trigger>
            <Trigger Property="ctrls:DropDownButton.Placement" Value="Right">
              <Setter TargetName="Arrow" Value="{x:Static styles:Geometries.ChevronRightSmall}" Property="Path.Data"/>
            </Trigger>
            <Trigger Property="ctrls:DropDownButton.Placement" Value="Top">
              <Setter TargetName="Arrow" Value="{x:Static styles:Geometries.ChevronUpSmall}" Property="Path.Data"/>
            </Trigger>
            <Trigger Property="ctrls:DropDownButton.ShowDropDownGlyph" Value="False">
              <Setter TargetName="Arrow" Property="UIElement.Visibility" Value="Collapsed"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
